{
  "name": "investmentscontract",
  "version": "1.0.0",
  "description": "Decentralized venture capital demo built with Truffle and Bootstrap.",
  "main": "truffle-config.js",
  "scripts": {
    "dev": "lite-server",
    "compile": "npx truffle compile",
    "test": "npx truffle test",
    "prepare:pages": "mkdir -p client && cp build/contracts/InvestmentsContract.json client/InvestmentsContract.json",
    "build:pages": "npm run prepare:pages",
    "deploy:sepolia": "npx truffle migrate --reset --network sepolia",
    "publish:pages": "npm run deploy:sepolia && npm run build:pages"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "directories": {
    "test": "test"
  },
  "dependencies": {
    "@truffle/hdwallet-provider": "^2.1.15",
    "@truffle/contract": "^4.4.11",
    "bootstrap": "^5.1.3",
    "bootstrap-icons": "^1.8.1",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "lite-server": "^2.6.1"
  }
}
